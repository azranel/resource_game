# Huntertest README

## Versions

```shell
rails 7
sqlite
rspec 
devise x.x
```

## Installation

```shell
bundle install
rails db:migrate
./bin/dev # This runs rails server, CSS processor, esbuild etc using Foreman
```
